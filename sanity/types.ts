/**
 * ---------------------------------------------------------------------------------
 * This file has been generated by Sanity TypeGen.
 * Command: `sanity typegen generate`
 *
 * Any modifications made directly to this file will be overwritten the next time
 * the TypeScript definitions are generated. Please make changes to the Sanity
 * schema definitions and/or GROQ queries if you need to update these types.
 *
 * For more information on how to use Sanity TypeGen, visit the official documentation:
 * https://www.sanity.io/docs/sanity-typegen
 * ---------------------------------------------------------------------------------
 */

export declare const internalGroqTypeReferenceTo: unique symbol;

// Source: sanity/extract.json
export type SanityImageAssetReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "sanity.imageAsset";
};

export type Seo = {
  _type: "seo";
  metaTitle?: string;
  metaDescription?: string;
  ogImage?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    _type: "image";
  };
  noIndex?: boolean;
};

export type SimpleText = Array<{
  children?: Array<{
    marks?: Array<string>;
    text?: string;
    _type: "span";
    _key: string;
  }>;
  style?: "normal";
  listItem?: never;
  markDefs?: Array<{
    href?: string;
    _type: "link";
    _key: string;
  }>;
  level?: number;
  _type: "block";
  _key: string;
}>;

export type Headline = Array<{
  children?: Array<{
    marks?: Array<string>;
    text?: string;
    _type: "span";
    _key: string;
  }>;
  style?: "normal";
  listItem?: never;
  markDefs?: null;
  level?: number;
  _type: "block";
  _key: string;
}>;

export type Art = {
  _type: "art";
  scene:
    | "skyline"
    | "riad"
    | "coast"
    | "savanna"
    | "vineyard"
    | "desert"
    | "river"
    | "lake";
  image?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
};

export type Button = {
  _type: "button";
  label: string;
  variant?: "primary" | "ghost" | "default";
  link: Link;
};

export type PageReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "page";
};

export type Link = {
  _type: "link";
  linkType: "internal" | "anchor" | "external" | "concierge";
  page?: PageReference;
  anchor?: string;
  url?: string;
  openInNewTab?: boolean;
};

export type CtaBanner = {
  _type: "ctaBanner";
  eyebrow?: string;
  title: string;
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
  tone: "cream" | "teal" | "sunset";
  anchorId?: string;
  hidden?: boolean;
};

export type EventList = {
  _type: "eventList";
  eyebrow?: string;
  title: string;
  aside?: string;
  showPast?: boolean;
  limit?: number;
  rsvpLabel?: string;
  emptyMessage?: string;
  tone: "cream" | "teal" | "sunset";
  anchorId?: string;
  hidden?: boolean;
};

export type MortgageCalculator = {
  _type: "mortgageCalculator";
  eyebrow?: string;
  title: string;
  aside?: string;
  defaultPrice?: number;
  defaultDeposit?: number;
  defaultRate?: number;
  defaultTerm?: number;
  resultLabel?: string;
  note?: string;
  tone: "cream" | "teal" | "sunset";
  anchorId?: string;
  hidden?: boolean;
};

export type CardGrid = {
  _type: "cardGrid";
  eyebrow?: string;
  title: string;
  cards?: Array<{
    title: string;
    body?: string;
    art?: Art;
    _type: "card";
    _key: string;
  }>;
  columns?: 2 | 3 | 4;
  tone: "cream" | "teal" | "sunset";
  anchorId?: string;
  hidden?: boolean;
};

export type Editorial = {
  _type: "editorial";
  eyebrow?: string;
  title: string;
  body?: SimpleText;
  pills?: Array<string>;
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
  art: Art;
  artPosition?: "left" | "right";
  tone: "cream" | "teal" | "sunset";
  anchorId?: string;
  hidden?: boolean;
};

export type FeatureList = {
  _type: "featureList";
  eyebrow?: string;
  title: string;
  aside?: string;
  items?: Array<{
    title: string;
    body?: string;
    _type: "feature";
    _key: string;
  }>;
  tone: "cream" | "teal" | "sunset";
  anchorId?: string;
  hidden?: boolean;
};

export type PropertyReference = {
  _ref: string;
  _type: "reference";
  _weak?: boolean;
  [internalGroqTypeReferenceTo]?: "property";
};

export type ListingGrid = {
  _type: "listingGrid";
  eyebrow?: string;
  title: string;
  aside?: string;
  source?: "all" | "selected";
  properties?: Array<
    {
      _key: string;
    } & PropertyReference
  >;
  showSceneTags?: boolean;
  tone: "cream" | "teal" | "sunset";
  anchorId?: string;
  hidden?: boolean;
};

export type StatsBar = {
  _type: "statsBar";
  tone: "cream" | "teal" | "sunset";
  hidden?: boolean;
  stats?: Array<{
    kind?: "custom" | "listingCount" | "countryCount" | "topPrice";
    value?: string;
    label: string;
    _type: "stat";
    _key: string;
  }>;
};

export type Hero = {
  _type: "hero";
  eyebrow?: string;
  title: Headline;
  lede?: SimpleText;
  buttons?: Array<
    {
      _key: string;
    } & Button
  >;
  art: Art;
  size?: "full" | "tall" | "compact";
  showReelStrip?: boolean;
  hidden?: boolean;
};

export type Event = {
  _id: string;
  _type: "event";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string;
  date: string;
  city: string;
  description?: string;
  rsvp?: Link;
};

export type Property = {
  _id: string;
  _type: "property";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  name: string;
  slug: Slug;
  price: number;
  currency:
    "USD" | "EUR" | "GBP" | "ZAR" | "KES" | "NGN" | "GHS" | "MAD" | "EGP";
  type:
    | "Villa"
    | "Penthouse"
    | "Apartment"
    | "Estate"
    | "Riad"
    | "Townhouse"
    | "Beach House";
  beds: number;
  city: string;
  country: string;
  region:
    | "East Africa"
    | "West Africa"
    | "North Africa"
    | "Southern Africa"
    | "Central Africa";
  tag?: string;
  art?: Art;
  gallery?: Array<
    {
      _key: string;
    } & Art
  >;
  badge?: string;
  sortOrder?: number;
};

export type Slug = {
  _type: "slug";
  current: string;
  source?: string;
};

export type SiteSettings = {
  _id: string;
  _type: "siteSettings";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string;
  homePage: PageReference;
  logo?: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    alt?: string;
    _type: "image";
  };
  wordmark?: string;
  wordmarkTagline?: string;
  navigation?: Array<{
    label: string;
    page: PageReference;
    color?: "grape" | "saffron" | "jungle" | "flame" | "ink";
    _type: "navItem";
    _key: string;
  }>;
  headerCta?: Button;
  footer?: {
    blurb?: string;
    exploreHeading?: string;
    regionsHeading?: string;
    regions?: Array<string>;
    contactHeading?: string;
    contactLines?: Array<string>;
    copyright?: string;
    defaultNote?: string;
  };
  concierge?: {
    enabled?: boolean;
    launcherTitle?: string;
    launcherSubtitle?: string;
    panelTitle?: string;
    panelSubtitle?: string;
    greeting?: SimpleText;
    inputPlaceholder?: string;
    budgetBands?: Array<{
      label: string;
      min?: number;
      max?: number;
      _type: "budgetBand";
      _key: string;
    }>;
    typeGroups?: Array<{
      label: string;
      types?: Array<string>;
      _type: "typeGroup";
      _key: string;
    }>;
    anywhereLabel?: string;
    callbackMessage?: string;
  };
  seo?: {
    defaultTitle?: string;
    description?: string;
    ogImage?: {
      asset?: SanityImageAssetReference;
      media?: unknown;
      hotspot?: SanityImageHotspot;
      crop?: SanityImageCrop;
      _type: "image";
    };
  };
};

export type SanityImageCrop = {
  _type: "sanity.imageCrop";
  top: number;
  bottom: number;
  left: number;
  right: number;
};

export type SanityImageHotspot = {
  _type: "sanity.imageHotspot";
  x: number;
  y: number;
  height: number;
  width: number;
};

export type Page = {
  _id: string;
  _type: "page";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  title: string;
  slug: Slug;
  pageBuilder?: Array<
    | ({
        _key: string;
      } & Hero)
    | ({
        _key: string;
      } & StatsBar)
    | ({
        _key: string;
      } & ListingGrid)
    | ({
        _key: string;
      } & FeatureList)
    | ({
        _key: string;
      } & Editorial)
    | ({
        _key: string;
      } & CardGrid)
    | ({
        _key: string;
      } & MortgageCalculator)
    | ({
        _key: string;
      } & EventList)
    | ({
        _key: string;
      } & CtaBanner)
  >;
  headerCta?: Button;
  footerNote?: string;
  seo?: Seo;
};

export type SanityImagePaletteSwatch = {
  _type: "sanity.imagePaletteSwatch";
  background?: string;
  foreground?: string;
  population?: number;
  title?: string;
};

export type SanityImagePalette = {
  _type: "sanity.imagePalette";
  darkMuted?: SanityImagePaletteSwatch;
  lightVibrant?: SanityImagePaletteSwatch;
  darkVibrant?: SanityImagePaletteSwatch;
  vibrant?: SanityImagePaletteSwatch;
  dominant?: SanityImagePaletteSwatch;
  lightMuted?: SanityImagePaletteSwatch;
  muted?: SanityImagePaletteSwatch;
};

export type SanityImageDimensions = {
  _type: "sanity.imageDimensions";
  height: number;
  width: number;
  aspectRatio: number;
};

export type SanityImageMetadata = {
  _type: "sanity.imageMetadata";
  location?: Geopoint;
  dimensions?: SanityImageDimensions;
  palette?: SanityImagePalette;
  lqip?: string;
  blurHash?: string;
  thumbHash?: string;
  hasAlpha?: boolean;
  isOpaque?: boolean;
};

export type SanityFileAsset = {
  _id: string;
  _type: "sanity.fileAsset";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  originalFilename?: string;
  label?: string;
  title?: string;
  description?: string;
  altText?: string;
  sha1hash: string;
  extension: string;
  mimeType: string;
  size: number;
  assetId: string;
  uploadId?: string;
  path: string;
  url: string;
  source?: SanityAssetSourceData;
};

export type SanityAssetSourceData = {
  _type: "sanity.assetSourceData";
  name?: string;
  id?: string;
  url?: string;
};

export type SanityImageAsset = {
  _id: string;
  _type: "sanity.imageAsset";
  _createdAt: string;
  _updatedAt: string;
  _rev: string;
  originalFilename?: string;
  label?: string;
  title?: string;
  description?: string;
  altText?: string;
  sha1hash: string;
  extension: string;
  mimeType: string;
  size: number;
  assetId: string;
  uploadId?: string;
  path: string;
  url: string;
  metadata?: SanityImageMetadata;
  source?: SanityAssetSourceData;
};

export type Geopoint = {
  _type: "geopoint";
  lat?: number;
  lng?: number;
  alt?: number;
};

export type AllSanitySchemaTypes =
  | SanityImageAssetReference
  | Seo
  | SimpleText
  | Headline
  | Art
  | Button
  | PageReference
  | Link
  | CtaBanner
  | EventList
  | MortgageCalculator
  | CardGrid
  | Editorial
  | FeatureList
  | PropertyReference
  | ListingGrid
  | StatsBar
  | Hero
  | Event
  | Property
  | Slug
  | SiteSettings
  | SanityImageCrop
  | SanityImageHotspot
  | Page
  | SanityImagePaletteSwatch
  | SanityImagePalette
  | SanityImageDimensions
  | SanityImageMetadata
  | SanityFileAsset
  | SanityAssetSourceData
  | SanityImageAsset
  | Geopoint;

// Source: sanity/lib/queries.ts
// Variable: HOME_PAGE_QUERY
// Query: *[_type == "page" && _id == *[_id == "siteSettings"][0].homePage._ref][0]{  _id,  _type,  title,  "slug": slug.current,  pageBuilder[]{    ...,    _type == "hero" => { buttons[] { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} }, art { scene, image { asset, crop, hotspot, alt } } },    _type == "editorial" => { buttons[] { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} }, art { scene, image { asset, crop, hotspot, alt } } },    _type == "ctaBanner" => { buttons[] { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} } },    _type == "cardGrid" => { cards[]{ _key, title, body, art { scene, image { asset, crop, hotspot, alt } } } },    _type == "listingGrid" => {      "listings": select(        source == "selected" => properties[]->{  _id,  name,  "slug": slug.current,  price,  currency,  type,  beds,  city,  country,  region,  tag,  badge,  art { scene, image { asset, crop, hotspot, alt } },  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }},        *[_type == "property" && defined(name)] | order(coalesce(sortOrder, 9999) asc, price desc) {  _id,  name,  "slug": slug.current,  price,  currency,  type,  beds,  city,  country,  region,  tag,  badge,  art { scene, image { asset, crop, hotspot, alt } },  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }}      )    },    _type == "eventList" => {      "events": *[        _type == "event" && defined(date) && (^.showPast == true || date >= $today)      ] | order(date asc) { _id, title, date, city, description, rsvp {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} }    },    _type == "statsBar" => {      "live": {        "listingCount": count(*[_type == "property"]),        "countryCount": count(array::unique(*[_type == "property"].country)),        "topPrice": math::max(*[_type == "property"].price)      }    }  }}
export type HOME_PAGE_QUERY_RESULT = {
  _id: string;
  _type: "page";
  title: string;
  slug: string;
  pageBuilder: Array<
    | {
        _key: string;
        _type: "cardGrid";
        eyebrow?: string;
        title: string;
        cards: Array<{
          _key: string;
          title: string;
          body: string | null;
          art: {
            scene:
              | "coast"
              | "desert"
              | "lake"
              | "riad"
              | "river"
              | "savanna"
              | "skyline"
              | "vineyard";
            image: {
              asset: SanityImageAssetReference | null;
              crop: SanityImageCrop | null;
              hotspot: SanityImageHotspot | null;
              alt: string | null;
            } | null;
          } | null;
        }> | null;
        columns?: 2 | 3 | 4;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "ctaBanner";
        eyebrow?: string;
        title: string;
        buttons: Array<{
          _key: string;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        }> | null;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "editorial";
        eyebrow?: string;
        title: string;
        body?: SimpleText;
        pills?: Array<string>;
        buttons: Array<{
          _key: string;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        }> | null;
        art: {
          scene:
            | "coast"
            | "desert"
            | "lake"
            | "riad"
            | "river"
            | "savanna"
            | "skyline"
            | "vineyard";
          image: {
            asset: SanityImageAssetReference | null;
            crop: SanityImageCrop | null;
            hotspot: SanityImageHotspot | null;
            alt: string | null;
          } | null;
        };
        artPosition?: "left" | "right";
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "eventList";
        eyebrow?: string;
        title: string;
        aside?: string;
        showPast?: boolean;
        limit?: number;
        rsvpLabel?: string;
        emptyMessage?: string;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
        events: Array<{
          _id: string;
          title: string;
          date: string;
          city: string;
          description: string | null;
          rsvp: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          } | null;
        }>;
      }
    | {
        _key: string;
        _type: "featureList";
        eyebrow?: string;
        title: string;
        aside?: string;
        items?: Array<{
          title: string;
          body?: string;
          _type: "feature";
          _key: string;
        }>;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "hero";
        eyebrow?: string;
        title: Headline;
        lede?: SimpleText;
        buttons: Array<{
          _key: string;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        }> | null;
        art: {
          scene:
            | "coast"
            | "desert"
            | "lake"
            | "riad"
            | "river"
            | "savanna"
            | "skyline"
            | "vineyard";
          image: {
            asset: SanityImageAssetReference | null;
            crop: SanityImageCrop | null;
            hotspot: SanityImageHotspot | null;
            alt: string | null;
          } | null;
        };
        size?: "compact" | "full" | "tall";
        showReelStrip?: boolean;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "listingGrid";
        eyebrow?: string;
        title: string;
        aside?: string;
        source?: "all" | "selected";
        properties?: Array<
          {
            _key: string;
          } & PropertyReference
        >;
        showSceneTags?: boolean;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
        listings: Array<{
          _id: string;
          name: string;
          slug: string;
          price: number;
          currency:
            | "EGP"
            | "EUR"
            | "GBP"
            | "GHS"
            | "KES"
            | "MAD"
            | "NGN"
            | "USD"
            | "ZAR";
          type:
            | "Apartment"
            | "Beach House"
            | "Estate"
            | "Penthouse"
            | "Riad"
            | "Townhouse"
            | "Villa";
          beds: number;
          city: string;
          country: string;
          region:
            | "Central Africa"
            | "East Africa"
            | "North Africa"
            | "Southern Africa"
            | "West Africa";
          tag: string | null;
          badge: string | null;
          art: {
            scene:
              | "coast"
              | "desert"
              | "lake"
              | "riad"
              | "river"
              | "savanna"
              | "skyline"
              | "vineyard";
            image: {
              asset: SanityImageAssetReference | null;
              crop: SanityImageCrop | null;
              hotspot: SanityImageHotspot | null;
              alt: string | null;
            } | null;
          } | null;
          gallery: Array<{
            _key: string;
            scene:
              | "coast"
              | "desert"
              | "lake"
              | "riad"
              | "river"
              | "savanna"
              | "skyline"
              | "vineyard";
            image: {
              asset: SanityImageAssetReference | null;
              crop: SanityImageCrop | null;
              hotspot: SanityImageHotspot | null;
              alt: string | null;
            } | null;
          }> | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "mortgageCalculator";
        eyebrow?: string;
        title: string;
        aside?: string;
        defaultPrice?: number;
        defaultDeposit?: number;
        defaultRate?: number;
        defaultTerm?: number;
        resultLabel?: string;
        note?: string;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "statsBar";
        tone: "cream" | "sunset" | "teal";
        hidden?: boolean;
        stats?: Array<{
          kind?: "countryCount" | "custom" | "listingCount" | "topPrice";
          value?: string;
          label: string;
          _type: "stat";
          _key: string;
        }>;
        live: {
          listingCount: number;
          countryCount: number;
          topPrice: number;
        };
      }
  > | null;
} | null;

// Source: sanity/lib/queries.ts
// Variable: PAGE_QUERY
// Query: *[_type == "page" && slug.current == $slug][0]{  _id,  _type,  title,  "slug": slug.current,  pageBuilder[]{    ...,    _type == "hero" => { buttons[] { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} }, art { scene, image { asset, crop, hotspot, alt } } },    _type == "editorial" => { buttons[] { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} }, art { scene, image { asset, crop, hotspot, alt } } },    _type == "ctaBanner" => { buttons[] { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} } },    _type == "cardGrid" => { cards[]{ _key, title, body, art { scene, image { asset, crop, hotspot, alt } } } },    _type == "listingGrid" => {      "listings": select(        source == "selected" => properties[]->{  _id,  name,  "slug": slug.current,  price,  currency,  type,  beds,  city,  country,  region,  tag,  badge,  art { scene, image { asset, crop, hotspot, alt } },  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }},        *[_type == "property" && defined(name)] | order(coalesce(sortOrder, 9999) asc, price desc) {  _id,  name,  "slug": slug.current,  price,  currency,  type,  beds,  city,  country,  region,  tag,  badge,  art { scene, image { asset, crop, hotspot, alt } },  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }}      )    },    _type == "eventList" => {      "events": *[        _type == "event" && defined(date) && (^.showPast == true || date >= $today)      ] | order(date asc) { _id, title, date, city, description, rsvp {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} }    },    _type == "statsBar" => {      "live": {        "listingCount": count(*[_type == "property"]),        "countryCount": count(array::unique(*[_type == "property"].country)),        "topPrice": math::max(*[_type == "property"].price)      }    }  }}
export type PAGE_QUERY_RESULT = {
  _id: string;
  _type: "page";
  title: string;
  slug: string;
  pageBuilder: Array<
    | {
        _key: string;
        _type: "cardGrid";
        eyebrow?: string;
        title: string;
        cards: Array<{
          _key: string;
          title: string;
          body: string | null;
          art: {
            scene:
              | "coast"
              | "desert"
              | "lake"
              | "riad"
              | "river"
              | "savanna"
              | "skyline"
              | "vineyard";
            image: {
              asset: SanityImageAssetReference | null;
              crop: SanityImageCrop | null;
              hotspot: SanityImageHotspot | null;
              alt: string | null;
            } | null;
          } | null;
        }> | null;
        columns?: 2 | 3 | 4;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "ctaBanner";
        eyebrow?: string;
        title: string;
        buttons: Array<{
          _key: string;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        }> | null;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "editorial";
        eyebrow?: string;
        title: string;
        body?: SimpleText;
        pills?: Array<string>;
        buttons: Array<{
          _key: string;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        }> | null;
        art: {
          scene:
            | "coast"
            | "desert"
            | "lake"
            | "riad"
            | "river"
            | "savanna"
            | "skyline"
            | "vineyard";
          image: {
            asset: SanityImageAssetReference | null;
            crop: SanityImageCrop | null;
            hotspot: SanityImageHotspot | null;
            alt: string | null;
          } | null;
        };
        artPosition?: "left" | "right";
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "eventList";
        eyebrow?: string;
        title: string;
        aside?: string;
        showPast?: boolean;
        limit?: number;
        rsvpLabel?: string;
        emptyMessage?: string;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
        events: Array<{
          _id: string;
          title: string;
          date: string;
          city: string;
          description: string | null;
          rsvp: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          } | null;
        }>;
      }
    | {
        _key: string;
        _type: "featureList";
        eyebrow?: string;
        title: string;
        aside?: string;
        items?: Array<{
          title: string;
          body?: string;
          _type: "feature";
          _key: string;
        }>;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "hero";
        eyebrow?: string;
        title: Headline;
        lede?: SimpleText;
        buttons: Array<{
          _key: string;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        }> | null;
        art: {
          scene:
            | "coast"
            | "desert"
            | "lake"
            | "riad"
            | "river"
            | "savanna"
            | "skyline"
            | "vineyard";
          image: {
            asset: SanityImageAssetReference | null;
            crop: SanityImageCrop | null;
            hotspot: SanityImageHotspot | null;
            alt: string | null;
          } | null;
        };
        size?: "compact" | "full" | "tall";
        showReelStrip?: boolean;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "listingGrid";
        eyebrow?: string;
        title: string;
        aside?: string;
        source?: "all" | "selected";
        properties?: Array<
          {
            _key: string;
          } & PropertyReference
        >;
        showSceneTags?: boolean;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
        listings: Array<{
          _id: string;
          name: string;
          slug: string;
          price: number;
          currency:
            | "EGP"
            | "EUR"
            | "GBP"
            | "GHS"
            | "KES"
            | "MAD"
            | "NGN"
            | "USD"
            | "ZAR";
          type:
            | "Apartment"
            | "Beach House"
            | "Estate"
            | "Penthouse"
            | "Riad"
            | "Townhouse"
            | "Villa";
          beds: number;
          city: string;
          country: string;
          region:
            | "Central Africa"
            | "East Africa"
            | "North Africa"
            | "Southern Africa"
            | "West Africa";
          tag: string | null;
          badge: string | null;
          art: {
            scene:
              | "coast"
              | "desert"
              | "lake"
              | "riad"
              | "river"
              | "savanna"
              | "skyline"
              | "vineyard";
            image: {
              asset: SanityImageAssetReference | null;
              crop: SanityImageCrop | null;
              hotspot: SanityImageHotspot | null;
              alt: string | null;
            } | null;
          } | null;
          gallery: Array<{
            _key: string;
            scene:
              | "coast"
              | "desert"
              | "lake"
              | "riad"
              | "river"
              | "savanna"
              | "skyline"
              | "vineyard";
            image: {
              asset: SanityImageAssetReference | null;
              crop: SanityImageCrop | null;
              hotspot: SanityImageHotspot | null;
              alt: string | null;
            } | null;
          }> | null;
        }> | null;
      }
    | {
        _key: string;
        _type: "mortgageCalculator";
        eyebrow?: string;
        title: string;
        aside?: string;
        defaultPrice?: number;
        defaultDeposit?: number;
        defaultRate?: number;
        defaultTerm?: number;
        resultLabel?: string;
        note?: string;
        tone: "cream" | "sunset" | "teal";
        anchorId?: string;
        hidden?: boolean;
      }
    | {
        _key: string;
        _type: "statsBar";
        tone: "cream" | "sunset" | "teal";
        hidden?: boolean;
        stats?: Array<{
          kind?: "countryCount" | "custom" | "listingCount" | "topPrice";
          value?: string;
          label: string;
          _type: "stat";
          _key: string;
        }>;
        live: {
          listingCount: number;
          countryCount: number;
          topPrice: number;
        };
      }
  > | null;
} | null;

// Source: sanity/lib/queries.ts
// Variable: HOME_SEO_QUERY
// Query: *[_type == "page" && _id == *[_id == "siteSettings"][0].homePage._ref][0]{  title,  seo{ metaTitle, metaDescription, noIndex, ogImage },  "siteName": *[_id == "siteSettings"][0].title,  "defaultOgImage": *[_id == "siteSettings"][0].seo.ogImage}
export type HOME_SEO_QUERY_RESULT = {
  title: string;
  seo: {
    metaTitle: string | null;
    metaDescription: string | null;
    noIndex: boolean | null;
    ogImage: {
      asset?: SanityImageAssetReference;
      media?: unknown;
      hotspot?: SanityImageHotspot;
      crop?: SanityImageCrop;
      _type: "image";
    } | null;
  } | null;
  siteName: string | null;
  defaultOgImage: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    _type: "image";
  } | null;
} | null;

// Source: sanity/lib/queries.ts
// Variable: PAGE_SEO_QUERY
// Query: *[_type == "page" && slug.current == $slug][0]{  title,  seo{ metaTitle, metaDescription, noIndex, ogImage },  "siteName": *[_id == "siteSettings"][0].title,  "defaultOgImage": *[_id == "siteSettings"][0].seo.ogImage}
export type PAGE_SEO_QUERY_RESULT = {
  title: string;
  seo: {
    metaTitle: string | null;
    metaDescription: string | null;
    noIndex: boolean | null;
    ogImage: {
      asset?: SanityImageAssetReference;
      media?: unknown;
      hotspot?: SanityImageHotspot;
      crop?: SanityImageCrop;
      _type: "image";
    } | null;
  } | null;
  siteName: string | null;
  defaultOgImage: {
    asset?: SanityImageAssetReference;
    media?: unknown;
    hotspot?: SanityImageHotspot;
    crop?: SanityImageCrop;
    _type: "image";
  } | null;
} | null;

// Source: sanity/lib/queries.ts
// Variable: HOME_SLUG_QUERY
// Query: *[_type == "page" && _id == *[_id == "siteSettings"][0].homePage._ref][0].slug.current
export type HOME_SLUG_QUERY_RESULT = string | null;

// Source: sanity/lib/queries.ts
// Variable: PAGE_SLUGS_QUERY
// Query: *[_type == "page" && defined(slug.current) && _id != *[_id == "siteSettings"][0].homePage._ref]{ "slug": slug.current }
export type PAGE_SLUGS_QUERY_RESULT = Array<{
  slug: string;
}>;

// Source: sanity/lib/queries.ts
// Variable: SITEMAP_QUERY
// Query: *[_type == "page" && defined(slug.current) && seo.noIndex != true]{    "path": select(_id == *[_id == "siteSettings"][0].homePage._ref => "/", "/" + slug.current),    _updatedAt  }
export type SITEMAP_QUERY_RESULT = Array<{
  path: string | "/";
  _updatedAt: string;
}>;

// Source: sanity/lib/queries.ts
// Variable: LAYOUT_QUERY
// Query: {  "settings": *[_id == "siteSettings"][0]{    title,    wordmark,    wordmarkTagline,    logo{      alt,      "url": asset->url,      "width": asset->metadata.dimensions.width,      "height": asset->metadata.dimensions.height    },    headerCta { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} },    "conciergeEnabled": coalesce(concierge.enabled, false),    navigation[]{      _key,      label,      color,      "href": select(        page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",        defined(page->slug.current) => "/" + page->slug.current      )    },    footer{ blurb, exploreHeading, regionsHeading, regions, contactHeading, contactLines, copyright, defaultNote },    seo{ defaultTitle, description, ogImage }  },  "pages": *[_type == "page" && defined(slug.current)]{    _id,    "path": select(_id == *[_id == "siteSettings"][0].homePage._ref => "/", "/" + slug.current),    footerNote,    headerCta { _key, label, variant, link {  linkType,  anchor,  url,  openInNewTab,  "pageHref": select(    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",    defined(page->slug.current) => "/" + page->slug.current  )} }  },  "cities": array::unique(    *[_type == "property" && defined(city)] | order(coalesce(sortOrder, 9999) asc).city  )}
export type LAYOUT_QUERY_RESULT = {
  settings:
    | {
        title: string;
        wordmark: string | null;
        wordmarkTagline: string | null;
        logo: {
          alt: string | null;
          url: string | null;
          width: number | null;
          height: number | null;
        } | null;
        headerCta: {
          _key: null;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        } | null;
        conciergeEnabled: boolean | false;
        navigation: Array<{
          _key: string;
          label: string;
          color: "flame" | "grape" | "ink" | "jungle" | "saffron" | null;
          href: string | "/";
        }> | null;
        footer: {
          blurb: string | null;
          exploreHeading: string | null;
          regionsHeading: string | null;
          regions: Array<string> | null;
          contactHeading: string | null;
          contactLines: Array<string> | null;
          copyright: string | null;
          defaultNote: string | null;
        } | null;
        seo: {
          defaultTitle: string | null;
          description: string | null;
          ogImage: {
            asset?: SanityImageAssetReference;
            media?: unknown;
            hotspot?: SanityImageHotspot;
            crop?: SanityImageCrop;
            _type: "image";
          } | null;
        } | null;
      }
    | {
        title: null;
        wordmark: null;
        wordmarkTagline: null;
        logo: null;
        headerCta: null;
        conciergeEnabled: false;
        navigation: null;
        footer: null;
        seo: null;
      }
    | {
        title: string;
        wordmark: null;
        wordmarkTagline: null;
        logo: null;
        headerCta: null;
        conciergeEnabled: false;
        navigation: null;
        footer: null;
        seo: null;
      }
    | {
        title: string | null;
        wordmark: null;
        wordmarkTagline: null;
        logo: null;
        headerCta: null;
        conciergeEnabled: false;
        navigation: null;
        footer: null;
        seo: null;
      }
    | {
        title: string;
        wordmark: null;
        wordmarkTagline: null;
        logo: null;
        headerCta: {
          _key: null;
          label: string;
          variant: "default" | "ghost" | "primary" | null;
          link: {
            linkType: "anchor" | "concierge" | "external" | "internal";
            anchor: string | null;
            url: string | null;
            openInNewTab: boolean | null;
            pageHref: string | "/" | null;
          };
        } | null;
        conciergeEnabled: false;
        navigation: null;
        footer: null;
        seo: {
          defaultTitle: null;
          description: null;
          ogImage: {
            asset?: SanityImageAssetReference;
            media?: unknown;
            hotspot?: SanityImageHotspot;
            crop?: SanityImageCrop;
            _type: "image";
          } | null;
        } | null;
      }
    | null;
  pages: Array<{
    _id: string;
    path: string | "/";
    footerNote: string | null;
    headerCta: {
      _key: null;
      label: string;
      variant: "default" | "ghost" | "primary" | null;
      link: {
        linkType: "anchor" | "concierge" | "external" | "internal";
        anchor: string | null;
        url: string | null;
        openInNewTab: boolean | null;
        pageHref: string | "/" | null;
      };
    } | null;
  }>;
  cities: Array<string>;
};

// Source: sanity/lib/queries.ts
// Variable: OG_QUERY
// Query: *[_id == "siteSettings"][0]{  title,  "tagline": seo.description}
export type OG_QUERY_RESULT =
  | {
      title: null;
      tagline: null;
    }
  | {
      title: string;
      tagline: null;
    }
  | {
      title: string | null;
      tagline: null;
    }
  | {
      title: string;
      tagline: string | null;
    }
  | null;

// Source: sanity/lib/queries.ts
// Variable: CONCIERGE_QUERY
// Query: {  "config": *[_id == "siteSettings"][0].concierge{    launcherTitle,    launcherSubtitle,    panelTitle,    panelSubtitle,    greeting,    inputPlaceholder,    budgetBands[]{ _key, label, min, max },    typeGroups[]{ _key, label, types },    anywhereLabel,    callbackMessage  },  "properties": *[_type == "property" && defined(name) && defined(price)]    | order(coalesce(sortOrder, 9999) asc) {      _id, name, price, currency, type, beds, city, country, region, tag    }}
export type CONCIERGE_QUERY_RESULT = {
  config: null | {
    launcherTitle: string | null;
    launcherSubtitle: string | null;
    panelTitle: string | null;
    panelSubtitle: string | null;
    greeting: SimpleText | null;
    inputPlaceholder: string | null;
    budgetBands: Array<{
      _key: string;
      label: string;
      min: number | null;
      max: number | null;
    }> | null;
    typeGroups: Array<{
      _key: string;
      label: string;
      types: Array<string> | null;
    }> | null;
    anywhereLabel: string | null;
    callbackMessage: string | null;
  };
  properties: Array<{
    _id: string;
    name: string;
    price: number;
    currency:
      "EGP" | "EUR" | "GBP" | "GHS" | "KES" | "MAD" | "NGN" | "USD" | "ZAR";
    type:
      | "Apartment"
      | "Beach House"
      | "Estate"
      | "Penthouse"
      | "Riad"
      | "Townhouse"
      | "Villa";
    beds: number;
    city: string;
    country: string;
    region:
      | "Central Africa"
      | "East Africa"
      | "North Africa"
      | "Southern Africa"
      | "West Africa";
    tag: string | null;
  }>;
};

// Query TypeMap
declare global {
  interface SanityQueries {
    '*[_type == "page" && _id == *[_id == "siteSettings"][0].homePage._ref][0]{\n  _id,\n  _type,\n  title,\n  "slug": slug.current,\n  pageBuilder[]{\n    ...,\n    _type == "hero" => { buttons[] { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} }, art { scene, image { asset, crop, hotspot, alt } } },\n    _type == "editorial" => { buttons[] { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} }, art { scene, image { asset, crop, hotspot, alt } } },\n    _type == "ctaBanner" => { buttons[] { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} } },\n    _type == "cardGrid" => { cards[]{ _key, title, body, art { scene, image { asset, crop, hotspot, alt } } } },\n    _type == "listingGrid" => {\n      "listings": select(\n        source == "selected" => properties[]->{\n  _id,\n  name,\n  "slug": slug.current,\n  price,\n  currency,\n  type,\n  beds,\n  city,\n  country,\n  region,\n  tag,\n  badge,\n  art { scene, image { asset, crop, hotspot, alt } },\n  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }\n},\n        *[_type == "property" && defined(name)] | order(coalesce(sortOrder, 9999) asc, price desc) {\n  _id,\n  name,\n  "slug": slug.current,\n  price,\n  currency,\n  type,\n  beds,\n  city,\n  country,\n  region,\n  tag,\n  badge,\n  art { scene, image { asset, crop, hotspot, alt } },\n  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }\n}\n      )\n    },\n    _type == "eventList" => {\n      "events": *[\n        _type == "event" && defined(date) && (^.showPast == true || date >= $today)\n      ] | order(date asc) { _id, title, date, city, description, rsvp {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} }\n    },\n    _type == "statsBar" => {\n      "live": {\n        "listingCount": count(*[_type == "property"]),\n        "countryCount": count(array::unique(*[_type == "property"].country)),\n        "topPrice": math::max(*[_type == "property"].price)\n      }\n    }\n  }\n}': HOME_PAGE_QUERY_RESULT;
    '*[_type == "page" && slug.current == $slug][0]{\n  _id,\n  _type,\n  title,\n  "slug": slug.current,\n  pageBuilder[]{\n    ...,\n    _type == "hero" => { buttons[] { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} }, art { scene, image { asset, crop, hotspot, alt } } },\n    _type == "editorial" => { buttons[] { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} }, art { scene, image { asset, crop, hotspot, alt } } },\n    _type == "ctaBanner" => { buttons[] { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} } },\n    _type == "cardGrid" => { cards[]{ _key, title, body, art { scene, image { asset, crop, hotspot, alt } } } },\n    _type == "listingGrid" => {\n      "listings": select(\n        source == "selected" => properties[]->{\n  _id,\n  name,\n  "slug": slug.current,\n  price,\n  currency,\n  type,\n  beds,\n  city,\n  country,\n  region,\n  tag,\n  badge,\n  art { scene, image { asset, crop, hotspot, alt } },\n  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }\n},\n        *[_type == "property" && defined(name)] | order(coalesce(sortOrder, 9999) asc, price desc) {\n  _id,\n  name,\n  "slug": slug.current,\n  price,\n  currency,\n  type,\n  beds,\n  city,\n  country,\n  region,\n  tag,\n  badge,\n  art { scene, image { asset, crop, hotspot, alt } },\n  gallery[]{ _key, scene, image { asset, crop, hotspot, alt } }\n}\n      )\n    },\n    _type == "eventList" => {\n      "events": *[\n        _type == "event" && defined(date) && (^.showPast == true || date >= $today)\n      ] | order(date asc) { _id, title, date, city, description, rsvp {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} }\n    },\n    _type == "statsBar" => {\n      "live": {\n        "listingCount": count(*[_type == "property"]),\n        "countryCount": count(array::unique(*[_type == "property"].country)),\n        "topPrice": math::max(*[_type == "property"].price)\n      }\n    }\n  }\n}': PAGE_QUERY_RESULT;
    '*[_type == "page" && _id == *[_id == "siteSettings"][0].homePage._ref][0]{\n  title,\n  seo{ metaTitle, metaDescription, noIndex, ogImage },\n  "siteName": *[_id == "siteSettings"][0].title,\n  "defaultOgImage": *[_id == "siteSettings"][0].seo.ogImage\n}': HOME_SEO_QUERY_RESULT;
    '*[_type == "page" && slug.current == $slug][0]{\n  title,\n  seo{ metaTitle, metaDescription, noIndex, ogImage },\n  "siteName": *[_id == "siteSettings"][0].title,\n  "defaultOgImage": *[_id == "siteSettings"][0].seo.ogImage\n}': PAGE_SEO_QUERY_RESULT;
    '*[_type == "page" && _id == *[_id == "siteSettings"][0].homePage._ref][0].slug.current': HOME_SLUG_QUERY_RESULT;
    '*[_type == "page" && defined(slug.current) && _id != *[_id == "siteSettings"][0].homePage._ref]{ "slug": slug.current }': PAGE_SLUGS_QUERY_RESULT;
    '*[_type == "page" && defined(slug.current) && seo.noIndex != true]{\n    "path": select(_id == *[_id == "siteSettings"][0].homePage._ref => "/", "/" + slug.current),\n    _updatedAt\n  }': SITEMAP_QUERY_RESULT;
    '{\n  "settings": *[_id == "siteSettings"][0]{\n    title,\n    wordmark,\n    wordmarkTagline,\n    logo{\n      alt,\n      "url": asset->url,\n      "width": asset->metadata.dimensions.width,\n      "height": asset->metadata.dimensions.height\n    },\n    headerCta { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} },\n    "conciergeEnabled": coalesce(concierge.enabled, false),\n    navigation[]{\n      _key,\n      label,\n      color,\n      "href": select(\n        page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n        defined(page->slug.current) => "/" + page->slug.current\n      )\n    },\n    footer{ blurb, exploreHeading, regionsHeading, regions, contactHeading, contactLines, copyright, defaultNote },\n    seo{ defaultTitle, description, ogImage }\n  },\n  "pages": *[_type == "page" && defined(slug.current)]{\n    _id,\n    "path": select(_id == *[_id == "siteSettings"][0].homePage._ref => "/", "/" + slug.current),\n    footerNote,\n    headerCta { _key, label, variant, link {\n  linkType,\n  anchor,\n  url,\n  openInNewTab,\n  "pageHref": select(\n    page._ref == *[_id == "siteSettings"][0].homePage._ref => "/",\n    defined(page->slug.current) => "/" + page->slug.current\n  )\n} }\n  },\n  "cities": array::unique(\n    *[_type == "property" && defined(city)] | order(coalesce(sortOrder, 9999) asc).city\n  )\n}': LAYOUT_QUERY_RESULT;
    '*[_id == "siteSettings"][0]{\n  title,\n  "tagline": seo.description\n}': OG_QUERY_RESULT;
    '{\n  "config": *[_id == "siteSettings"][0].concierge{\n    launcherTitle,\n    launcherSubtitle,\n    panelTitle,\n    panelSubtitle,\n    greeting,\n    inputPlaceholder,\n    budgetBands[]{ _key, label, min, max },\n    typeGroups[]{ _key, label, types },\n    anywhereLabel,\n    callbackMessage\n  },\n  "properties": *[_type == "property" && defined(name) && defined(price)]\n    | order(coalesce(sortOrder, 9999) asc) {\n      _id, name, price, currency, type, beds, city, country, region, tag\n    }\n}': CONCIERGE_QUERY_RESULT;
  }
}
// Lets @sanity/client releases that predate the global registry read it too
declare module "@sanity/client" {
  interface SanityQueries extends globalThis.SanityQueries {}
}
